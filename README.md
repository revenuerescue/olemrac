# Olemrac

A budget that runs on your paydays instead of the calendar. Installable web app; all data stays on your device (IndexedDB).

Static site: `index.html`, `manifest.webmanifest`, `sw.js`, `icons/`. Deploy anywhere that serves static files.
